struct a {
    a1: u32, 
    a2: f32,
    a3: b,
}

struct b {
    b1: u32,
}

enum c {
    Test(f32),
    This(u32),
}

fn main() {
    let a: u32 = read_u32();
    let b: u32 = square(a);
    print(a, b);
}

fn square(a: u32) -> u32 {
    a * a
}

fn wrap(a: u32) -> b {
    
}