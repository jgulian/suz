ext read_int() -> usize;

ext print_int(usize) -> ();

fun main() -> usize {
    let a: usize = read_int();
    let b: usize = double(a);
    let c: usize = double(b);
    print_int(c)
}

fun double(a: usize) -> usize {
    a + a
}