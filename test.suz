struct a {
    a1: usize, 
    a2: fsize,
    a3: b
}

struct b {
    b1: usize
}

enum c {
    Test((usize, fsize)),
    This(usize)
}

fun main() {
    let a: usize = read_usize();
    let b: usize = square(a);
    print(a, b)
}

fun square(a: usize) -> b {
    wrap(a * a)
}

fun wrap(a: usize) -> b {
    B { b1: 1 }
}