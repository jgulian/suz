struct a {
    a1: usize, 
    a2: fsize,
    a3: b
}

struct b {
    b1: usize
}

fun main() -> usize {
    let a: usize = 1;
    let b: usize = square(a);
    let c: usize = square(b);
    square(c)
}

fun square(a: usize) -> usize {
    a * a
}

fun wrap(a: usize) -> b {
    b { b1: 1 }
}